using UnityEngine;
using NGAME;

public class SpawnerDemo : MonoBehaviour
{
    [SerializeField]
    private SpawnerData m_SpawnData = new();

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
